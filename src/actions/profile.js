import axios from 'axios';
import { setAlert } from './alerts';

import { GET_PROFILE, PROFILE_ERROR } from './types';

// Get curremt users profile

export const getCurrentUserProfile = () => async dispatchEvent => {};
